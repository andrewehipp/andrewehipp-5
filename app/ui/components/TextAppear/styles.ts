import { css } from '@/styled-system/css'

export const text = css({
    display: 'block',
    overflow: 'hidden',
    opacity: 1,
    transition: '300ms',
    _starting: {
        opacity: 0,
    }
});
